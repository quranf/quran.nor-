# امنیت «قرآن نور»

این سند می‌گوید چه مشکلاتی پیدا و رفع شد، و **چه کارهایی باید خودتان انجام دهید** (بخش ۳ مهم‌ترین بخش است).

## ۱) آسیب‌پذیری‌های پیدا‌شده و رفع‌شده

| # | شدت | مشکل | رفع |
|---|------|------|-----|
| 1 | **بحرانی** | نبودِ قوانین Firestore در پروژه. اگر دیتابیس در «حالت تست» باشد، هر کسی روی اینترنت می‌تواند سند هر کاربر را بخواند یا بنویسد. | فایل `security/firestore.rules` (هر کاربر فقط سند خودش، با ساختار مجاز). **باید منتشر شود (بخش ۳).** |
| 2 | **بالا** | DOM-XSS: مقدارهای `lastRead` / `readHistory` (از localStorage یا سند ابری) بدون فیلتر داخل `innerHTML` می‌رفتند. با تست واقعی روی نسخهٔ قبلی، کد دلخواه اجرا شد. ترکیب با مورد ۱ یعنی حملهٔ ذخیره‌شده روی حساب دیگران. | همهٔ داده‌های ورودی (localStorage، ابر، prefs، نشانه‌ها) با توابع `sanitize*` به عدد/بولین/مقدار مجاز تبدیل می‌شوند. |
| 3 | **بالا** | ورودِ «محلی» با رمز ذخیره‌شده در localStorage: هش SHA-256 با نمک ثابت، و اگر `crypto.subtle` نبود فقط `btoa` (یعنی رمز تقریباً خام). این مسیر هر وقت Firebase لود نمی‌شد فعال می‌شد. | کل مسیر حذف شد؛ رمز هیچ‌جا ذخیره نمی‌شود. هش‌های قدیمیِ روی دستگاه‌ها هنگام اجرا پاک می‌شوند. |
| 4 | متوسط | نبودِ CSP و هدرهای امنیتی؛ اسکریپت‌های درون‌خطی در `index.html`. | CSP سخت (بدون `unsafe-inline` برای اسکریپت)، `frame-ancestors 'none'`، HSTS، `nosniff`، `Referrer-Policy`، `Permissions-Policy`. هم در `index.html` (meta) و هم در `firebase.json` (هدر). |
| 5 | متوسط | پیام‌های ورود، ثبت‌شده‌بودن ایمیل را لو می‌دادند (user enumeration) و `e.message` خام نمایش داده می‌شد. | پیام‌های عمومی، قفل ۳۰ ثانیه‌ای بعد از ۵ تلاش ناموفق، بدون نمایش خطای داخلی. |
| 6 | متوسط | رمز حداقل ۶ کاراکتر، بدون بازیابی رمز و بدون تأیید ایمیل. | ثبت‌نام: حداقل ۸ کاراکتر و رد رمزهای رایج/عددی/شامل ایمیل (ورود کاربران قدیمی بسته نمی‌شود). دکمهٔ «فراموشی رمز عبور» و ایمیل تأیید اضافه شد. |
| 7 | متوسط | بعد از خروج، داده‌های کاربر روی کامپیوتر مشترک (مدرسه) می‌ماند؛ Firestore هم نسخهٔ آفلاین می‌ساخت. | بعد از همگام‌سازی موفق، نسخهٔ محلی حساب پاک می‌شود؛ `enablePersistence` حذف شد. |
| 8 | متوسط | فرم گزارش مشکل بدون هیچ محدودیت (اسپم)، با ایمیل شخصی در کد عمومی. | حداکثر طول، حذف کاراکتر کنترلی، ۱ گزارش در دقیقه، honeypot، timeout. **ایمیل هنوز در کد است؛ بخش ۳.** |
| 9 | پایین | `highlightSnippet` روی متن escape‌شده regex می‌زد (جستجوی «lt» و «amp» ساختار HTML را خراب می‌کرد). | اول علامت‌گذاری روی متن خام، بعد escape هر تکه. |
| 10 | پایین | متن «آیه روز» بدون escape؛ عبارت جستجو بدون سقف طول (بار CPU / ReDoS). | escape شد؛ سقف ۱۰۰ کاراکتر. |
| 11 | پایین | `loadScript` هر URL را می‌پذیرفت؛ صوت از هر URL؛ داده از هر مبدأ. | فهرست سفید میزبان برای اسکریپت و صوت؛ داده فقط از همان مبدأ. |
| 12 | افزوده | هیچ تضمینی نبود که متن قرآن دست‌نخورده باشد. | متن آیات دیگر فایل ثابتی روی هاست نیست؛ فقط از دیتابیس Firestore خوانده می‌شود و `security/firestore.rules` نوشتن از سمت کاربر را کاملاً می‌بندد (فقط Admin SDK/کنسول می‌تواند بنویسد) — یعنی کسی نمی‌تواند فایل را روی هاست دستکاری کند چون اصلاً فایلی نیست. |

## ۲) آزمون
`python3 security/security-tests.py` (نیاز به Playwright) — آزمون‌های XSS، CSP، رمز محلی، رمز ضعیف، سقف جستجو، و این‌که سوره از دیتابیس (Firestore) درست بارگذاری می‌شود. از این پس این آزمون‌ها به اینترنت واقعی و یک پروژهٔ Firebase پیکربندی‌شده با دادهٔ وارد‌شده نیاز دارند (چون متن آیات دیگر فایل محلی نیست). آزمون `tajweed-tests/verify.js` بدون تغییر است.

**محدودیت آزمون:** این تغییرات در محیطی بدون اینترنت آزمایش شدند؛ یعنی ورود واقعی با Firebase، قوانین Firestore و CSP روی سایت زندهٔ Firebase Hosting **آزمایش نشده‌اند**. بعد از انتشار حتماً بخش ۴ را انجام دهید.

## ۳) کارهایی که فقط خودتان می‌توانید انجام دهید

1. **منتشر کردن قوانین Firestore (فوری‌ترین):** Firebase Console → Firestore Database → Rules → محتوای `security/firestore.rules` را جایگزین کنید → Publish. قبل از آن در تب Rules Playground امتحان کنید.
2. **محدود کردن کلید API:** Google Cloud Console → APIs & Services → Credentials → کلید وب → Application restrictions = HTTP referrers، فقط دامنهٔ سایت خودتان (و `localhost` برای تست). (کلید Firebase وب مخفی نیست؛ محافظ واقعی قوانین و محدودیت‌هاست.)
3. **Authentication → Settings:** «Authorized domains» را فقط به دامنه‌های واقعی محدود کنید؛ «Email enumeration protection» را روشن کنید؛ در «Password policy» حداقل ۸ کاراکتر را الزامی کنید.
4. **ایمیل شخصی در کد:** در `app.js` ثابت `REPORT_ENDPOINT` هنوز نشانی ایمیل شما را دارد. در FormSubmit پس از تأیید ایمیل، «کد مستعار (random alias)» می‌دهد؛ آن را جایگزین کنید تا ایمیل شما در کد عمومی نباشد.
5. **قبل از انتشار در Google Play:** سیاست حریم خصوصی (Privacy Policy) با URL عمومی و فرم Data safety لازم است، چون ایمیل و رمز جمع می‌کنید (ایمیل، نشانه‌ها، سابقهٔ مطالعه).
6. **پیشنهاد بعدی (کار نشد چون به کلید/اینترنت نیاز دارد):** Firebase App Check؛ خودمیزبانی فونت‌ها و کتابخانهٔ Firebase (تا وابستگی به CDN و SRI ممکن شود).

## ۴) بعد از انتشار، این‌ها را امتحان کنید
ثبت‌نام، ورود، خروج، «فراموشی رمز»، همگام‌سازی نشانه‌ها بین دو دستگاه، پخش صوت و ارسال گزارش. اگر در Console مرورگر خطای «Refused to … Content Security Policy» دیدید، میزبان مربوط را به سیاست در `index.html` و `firebase.json` اضافه کنید (هر دو باید یکسان بمانند).

## ۵) نگهداری
- برای ویرایش متن یک سوره: مستقیم در Firebase Console → Firestore Database → مجموعهٔ `surahs` → سند مربوطه، یا با ویرایش فایل منبع و اجرای دوبارهٔ `scripts/import-to-firestore.js`.
- اسکریپت یا هندلر درون‌خطی (`onclick=…` یا `<script>…</script>`) در HTML نگذارید؛ CSP مسدودشان می‌کند.
- هر داده‌ای که از localStorage/ابر می‌آید را قبل از `innerHTML` با `escapeHtml` یا `sanitize*` عبور دهید.
